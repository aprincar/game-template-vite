{
  "name": "game-template-vite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "scripts": {
    "prepare": "node scripts/install-hooks.mjs",
    "dev": "vite",
    "build": "vite build",
    "check": "npm run test && npm run typecheck && npm run build && npm run package && npm run validate",
    "test": "node --test tests/*.test.mjs",
    "package": "node scripts/package.mjs",
    "validate": "node scripts/validate.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.7",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
